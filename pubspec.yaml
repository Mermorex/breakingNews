name: news_app
description: RSS News Reader
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  xml: ^6.3.0
  cached_network_image: ^3.3.0
  intl: ^0.19.0
  url_launcher: ^6.2.2
  shimmer: ^3.0.0
  html: ^0.15.6
  google_fonts: ^6.1.0
  rss_dart: ^1.0.14
  charset: ^2.0.0
  provider: ^6.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true